version: "3.8"
services:
  react-app:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: react-intro
    ports:
      - "80:80"  # nginx는 80 포트 사용
    restart: unless-stopped
    networks:
      - ext-network

networks:
  ext-network:
    external: true